fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
sqlalchemy==2.0.30
celery==5.4.0
redis==5.0.4
python-multipart==0.0.9
faster-whisper==1.2.1
ctranslate2==4.8.1
# faster-whisper's utils.py imports requests directly for model downloads, but
# neither faster-whisper nor huggingface-hub declares it as a dependency
# anymore (huggingface-hub moved to httpx internally) — pin it explicitly.
requests==2.32.3
scenedetect==0.6.4
numpy==1.26.4
opencv-python==4.11.0.86
rapidocr-onnxruntime==1.4.4
onnxruntime==1.22.1
# Storyboard/contact-sheet composition. OpenCV (already present via scenedetect)
# can only draw Hershey vector text — no TrueType, no wrapping — which is
# unreadable for frame captions, so Pillow does the compositing.
Pillow==10.4.0
# curl_cffi gives yt-dlp TLS browser-impersonation; the default extra pulls
# in the rest of its optional deps. 2026.8.19 matters specifically: it adds
# _generate_blockbuster_headers() to TikTok webpage requests — a new anti-bot
# header scheme TikTok began enforcing (intermittently) in Aug 2026. Without
# those headers TikTok serves a page with no embedded data and yt-dlp reports
# "Unable to extract universal data for rehydration"; 2026.7.4 failed exactly
# that way on and off depending on whether the check was enforced per-request.
yt-dlp[default,curl-cffi]==2026.8.19
python-json-logger==2.0.7
aiofiles==23.2.1
httpx==0.27.0
tenacity==8.3.0
